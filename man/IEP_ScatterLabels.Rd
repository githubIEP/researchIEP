% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IEP_ScatterLabels.R
\name{IEP_ScatterLabels}
\alias{IEP_ScatterLabels}
\title{Add Directional Labels to Scatter Plots}
\usage{
IEP_ScatterLabels(
  plot,
  xaxis = "Include",
  yaxis = "Include",
  left_text = "",
  right_text = "",
  up_text = "",
  down_text = "",
  xposition = 0.07,
  yposition = 0.045
)
}
\arguments{
\item{plot}{A ggplot object (typically a scatter plot)}

\item{xaxis}{Character. "Include" to show horizontal arrows and labels, "Exclude" to hide. Default "Include".}

\item{yaxis}{Character. "Include" to show vertical arrows and labels, "Exclude" to hide. Default "Include".}

\item{left_text}{Character. Label text for left direction. Default "".}

\item{right_text}{Character. Label text for right direction. Default "".}

\item{up_text}{Character. Label text for up direction. Default "".}

\item{down_text}{Character. Label text for down direction. Default "".}

\item{xposition}{Numeric. Vertical position of x-axis labels (0-1). Default 0.07.}

\item{yposition}{Numeric. Horizontal position of y-axis labels (0-1). Default 0.045.}
}
\value{
A plot object with directional labels and arrows added
}
\description{
Adds arrow indicators and text labels to show directional meaning on scatter plot axes.
Useful for creating quadrant charts or showing the meaning of axis directions.
}
\examples{
\dontrun{
library(ggplot2)

# Create a scatter plot
p <- ggplot(mtcars, aes(wt, mpg)) + 
  geom_point() +
  theme_minimal()

# Add directional labels
p_labeled <- IEP_ScatterLabels(
  p,
  left_text = "Lighter", 
  right_text = "Heavier",
  up_text = "More Efficient", 
  down_text = "Less Efficient"
)

# Add only horizontal labels
p_horizontal <- IEP_ScatterLabels(
  p,
  left_text = "Low", 
  right_text = "High",
  yaxis = "Exclude"
)
}
}
