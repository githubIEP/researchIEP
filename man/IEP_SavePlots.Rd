% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IEP_SavePlots.R
\name{IEP_SavePlots}
\alias{IEP_SavePlots}
\title{Save Plots in Multiple Formats and Sizes}
\usage{
IEP_SavePlots(
  plot_info,
  plot,
  sizes = NULL,
  paths = NULL,
  formats = c("png", "svg"),
  bg = "transparent",
  verbose = FALSE
)
}
\arguments{
\item{plot_info}{A list or named vector containing plot metadata:
\itemize{
\item type: "Chart", "Map", or "Diagram"
\item sheet: Base filename for the saved files
}}

\item{plot}{The plot object to save (ggplot, diagram, etc.)}

\item{sizes}{List of custom sizes. If NULL, uses global GPI_CHARTS/GPI_MAPS.}

\item{paths}{List of custom paths. If NULL, uses global paths.}

\item{formats}{Character vector of formats to save. Default c("png", "svg").}

\item{bg}{Background color for PNG files. Default "transparent".}

\item{verbose}{Logical. Print save locations? Default FALSE.}
}
\value{
Invisibly returns a list of saved file paths
}
\description{
Saves plots (charts, maps, or diagrams) in both PNG and SVG formats across
three standard sizes (small, medium, large) to designated output directories.
}
\examples{
\dontrun{
library(ggplot2)

# Create a sample plot
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()

# Define plot information
plot_info <- list(
  type = "Chart",
  sheet = "fuel_efficiency"
)

# Save in multiple sizes and formats
IEP_SavePlots(plot_info, p)

# Save with custom settings
IEP_SavePlots(plot_info, p, formats = "png", verbose = TRUE)
}
}
